---
title: "@ オートコンプリートにアイコンとシングルライン表示を改善"
date: 2026-01-13
tags: ['改善', 'UI', 'オートコンプリート', 'ユーザビリティ']
---

## 原文（日本語に翻訳）

@ オートコンプリートを改善し、異なる提案タイプ用のアイコンとシングルライン表示を追加

## 原文（英語）

Improved @ autocomplete with icons for different suggestion types and single-line formatting

## 概要

Claude Code v2.1.6 では、`@` オートコンプリート機能が改善されました。提案の種類（ファイル、フォルダ、MCP、コマンドなど）を視覚的に区別するアイコンが追加され、シングルライン表示により見やすさが向上しました。これにより、目的の項目を素早く識別して選択できます。

## 基本的な使い方

1. 入力欄で `@` を入力
2. オートコンプリートメニューが表示される
3. 各項目にアイコンが表示され、種類を識別できる
4. 矢印キーで選択、Enterで確定

```bash
# ファイルを添付する場合
@ファイル名

# MCPサーバーを有効化する場合
@mcp-server-name
```

## 実践例

### ファイル添付

コンテキストにファイルを追加する場合：

1. `@` と入力
2. ファイルアイコン付きの候補が表示される
3. ファイル名の一部を入力して絞り込み
4. 目的のファイルを選択

提案タイプとアイコン：
- **ファイル**: 📄 アイコンで表示
- **ディレクトリ**: 📁 アイコンで表示
- **MCPサーバー**: 🔌 アイコンで表示
- **コマンド**: ⚡ アイコンで表示

### MCPサーバーの有効化

特定のMCPサーバーを有効化する場合：

1. `@` と入力
2. MCPサーバーアイコン付きの候補を探す
3. サーバー名を選択

注: v2.1.6 では`@`メンションによるMCPサーバーの有効化/無効化機能は削除されました。代わりに `/mcp enable <name>` を使用してください。

### 複数ファイルの添付

プロジェクトの複数のファイルをコンテキストに追加：

1. `@src/` と入力してディレクトリを指定
2. ディレクトリ内のファイル一覧が表示される
3. 必要なファイルを順次選択
4. アイコンで各項目の種類を確認しながら選択

### シングルライン表示の利点

長いファイルパスも1行で表示されるため：
- スクロールが減り、一度に多くの候補を確認できる
- 候補の比較が容易
- 選択が高速化

## 注意点

- この機能は Claude Code v2.1.6 で導入されました
- アイコンは端末のフォント設定により表示が異なる場合があります
- シングルライン表示により、長いパスは末尾が省略される場合があります
- `@` オートコンプリートは、ファイル、MCPサーバー、コマンドなど複数の種類の項目を提案します
- bash モードでは `@` 記号がファイルオートコンプリートを誤ってトリガーする問題が修正されています（v2.1.14）

## 関連情報

- [Changelog v2.1.6](https://github.com/anthropics/claude-code/releases/tag/v2.1.6)
- [Changelog v2.1.14](https://github.com/anthropics/claude-code/releases/tag/v2.1.14) - bash モードでの @ 記号の問題修正
- [Changelog v2.1.0](https://github.com/anthropics/claude-code/releases/tag/v2.1.0) - スラッシュコマンドオートコンプリートの追加
